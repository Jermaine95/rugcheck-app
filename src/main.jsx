import React from "react";
import ReactDOM from "react-dom/client";
import RugChecker from "./RugChecker.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RugChecker />
  </React.StrictMode>
);
